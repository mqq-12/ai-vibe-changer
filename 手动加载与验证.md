# Doubao Web Skin 手动加载与验证

## 一、加载扩展

### Chrome

1. 打开 `chrome://extensions/`；
2. 开启右上角的“开发者模式”；
3. 点击“加载已解压的扩展程序”；
4. 选择目录：

```text
E:\奇思妙想\ds\dist
```

### Edge

1. 打开 `edge://extensions/`；
2. 开启“开发人员模式”；
3. 点击“加载解压缩的扩展”；
4. 选择目录：

```text
E:\奇思妙想\ds\dist
```

## 二、首次验证

1. 打开 `https://www.doubao.com/`；
2. 如果页面之前已经打开，请按 `Ctrl+R` 刷新；
3. 点击浏览器工具栏中的 **Doubao Web Skin**；
4. 检查“页面状态”和“兼容评分”；
5. 点击“打开主题工作室”；
6. 先选择一个统一颜色组；
7. 选择一个背景图片；
8. 点击“保存并应用”，确认出现“保存成功”；
9. 返回 Doubao 页面观察效果。

## 三、必须测试的功能

请逐项确认：

- [ ] Doubao 页面能够正常加载；
- [ ] 左侧会话栏仍然可以点击和滚动；
- [ ] 新建对话按钮正常；
- [ ] 输入框可以输入、粘贴和换行；
- [ ] 发送按钮可以点击；
- [ ] 文件上传按钮正常；
- [ ] 深度思考按钮正常；
- [ ] 联网搜索按钮正常；
- [ ] 模型选择器可以打开；
- [ ] 历史会话可以切换，选中项不会变成白底白字；
- [ ] 用户问题气泡背景和边框颜色已改变；
- [ ] 深度思考/思考过程区域背景、边框和文字颜色已改变；
- [ ] 顶部不再显示“探索问题，也探索答案之外的世界”横幅；
- [ ] 保存成功或失败时会出现明确提示；
- [ ] 回答流式生成时页面没有明显卡顿；
- [ ] 代码块和文字可读；
- [ ] 下拉菜单和弹窗可读；
- [ ] 刷新页面后主题自动恢复；
- [ ] 关闭扩展开关后恢复原生界面。

## 四、兼容评分

```text
80～100：完整模式
60～79：安全模式
低于 60：暂不兼容
```

如果分数低于 80，通常说明侧边栏、主内容区或输入区域没有被完整识别。

## 五、发现问题后请提供什么

请告诉我：

1. 浏览器类型和版本；
2. 弹窗显示的兼容评分；
3. 哪一部分没有变化或显示错误；
4. 是否有按钮被遮挡；
5. 浏览器扩展页面是否显示错误；
6. 可以提供截图，但请隐藏真实对话内容。

如果需要进一步适配 DOM，可在 Doubao 页面按 `F12`，使用元素选择工具点击以下区域：

- 左侧会话栏最外层；
- 输入框最外层；
- 一条用户消息最外层；
- 一条 AI 消息最外层。

只需复制对应元素的开始标签或经过脱敏的结构，例如：

```html
<div class="..." data-testid="..." role="...">
```

不要提供：

- 真实聊天内容；
- Cookie；
- Authorization 信息；
- 登录令牌；
- 完整网页存档。

## 六、查看扩展错误

1. 打开 `chrome://extensions/` 或 `edge://extensions/`；
2. 找到 Doubao Web Skin；
3. 如果卡片上出现“错误”，点击查看；
4. 复制错误文本发给我；
5. 不需要复制 Doubao 页面中的聊天内容。

## 七、重新加载修改后的版本

后续代码调整并重新构建后：

1. 回到扩展管理页面；
2. 点击 Doubao Web Skin 卡片上的“重新加载”按钮；
3. 回到 Doubao 页面按 `Ctrl+R`；
4. 再次检查兼容评分和界面效果。


